/*
function insertarFila() {
    let newRow = document.createElement("tr") //creo elemento nueva fila

    let firstColumn = document.createElement("td") //creo una columna y pongo su valor, luego la otra
    firstColumn.innerHTML = "fila 3 - celda 1"
    let secondColumn = document.createElement("td")
    secondColumn.innerHTML = "fila 3 - celda 2"

    newRow.appendChild(firstColumn) //agrego ambas columnas a la nueva fila anteriormente creada
    newRow.appendChild(secondColumn)

    let table = document.getElementById("Taula") //obtengo la tabla existente
    table.appendChild(newRow) //le agrego la nueva fila, que adentro contendrá las dos columnas
}*/
