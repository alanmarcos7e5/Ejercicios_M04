function prenValorForm() { //TODO no mantiene el console log
    let formulary = document.getElementById("formulari1")
    let dataOfFormulary = formulary.getElementsByTagName("input")
    console.log(dataOfFormulary)
}